/**
 * SCMS - Users Page
 * renderUsers(), openUserModal(), saveUser(), deleteUserConfirm()
 * are all defined in settings.js which is loaded before this.
 */
// This file intentionally left minimal — all user functions are in settings.js
